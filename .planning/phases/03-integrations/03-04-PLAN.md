---
phase: 03-integrations
plan: "04"
type: execute
wave: 4
depends_on: ["03-03"]
files_modified: []
autonomous: false
requirements: [INT-01, INT-02, INT-03, INT-04]

user_setup:
  - service: google-sheets
    why: "INT-01 Sheets append cannot be tested without live credentials"
    env_vars:
      - name: GOOGLE_SERVICE_ACCOUNT_EMAIL
        source: "GCP Console → IAM → Service Accounts → your account → Email field"
      - name: GOOGLE_PRIVATE_KEY
        source: "GCP Console → Service Accounts → Keys → Add Key → Create new key → JSON → copy private_key value (keep \\n escapes as-is — do NOT convert to real newlines)"
    dashboard_config:
      - task: "Share the target spreadsheet with GOOGLE_SERVICE_ACCOUNT_EMAIL as Editor"
        location: "Open the Google Sheet → Share button → paste service account email → set to Editor → Send"
  - service: cron-job.org
    why: "INT-03 automatic retry requires a scheduler calling /api/cron/retry every 5 minutes"
    env_vars:
      - name: CRON_SECRET
        source: "Generate a random string: openssl rand -hex 32"
    dashboard_config:
      - task: "Create a cron job at cron-job.org: URL = https://leads.esqtools.com/api/cron/retry, method = GET, header X-Cron-Secret: {your CRON_SECRET value}, every 5 minutes"
        location: "https://cron-job.org → Dashboard → Create cronjob"

must_haves:
  truths:
    - "A POST to /api/webhook/[slug] with a valid lead creates a row in Google Sheets"
    - "A POST to /api/webhook/[slug] creates a DataCrazy POST or logs failure in SyncLog"
    - "A SyncLog row with status=failed and nextRetryAt in the past is retried when cron fires"
    - "GET /api/leads returns lead list with syncLogs showing destination and status"
  artifacts:
    - path: ".env"
      provides: "GOOGLE_SERVICE_ACCOUNT_EMAIL, GOOGLE_PRIVATE_KEY, CRON_SECRET injected"
      contains: "GOOGLE_SERVICE_ACCOUNT_EMAIL"
  key_links:
    - from: "cron-job.org"
      to: "https://leads.esqtools.com/api/cron/retry"
      via: "HTTP GET every 5 min with X-Cron-Secret header"
      pattern: "cron"
---

<objective>
Inject environment credentials, redeploy, and verify all four INT requirements against the live VPS.

Purpose: All integration code is wired. This plan confirms it works end-to-end: Sheets append fires on real lead ingestion, DataCrazy POST attempts are logged, cron retry route is reachable and authorized, and leads endpoint returns sync status.

Output: Live system with working Google Sheets integration, cron scheduler running, verified via curl.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/c/Users/wagne/Documents/Claude Code/n8n/Fluxo-captação/.planning/STATE.md
@/c/Users/wagne/Documents/Claude Code/n8n/Fluxo-captação/.planning/phases/03-integrations/03-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Inject env vars and redeploy on Easypanel</name>
  <what-to-do>
Add the following environment variables to the Easypanel service for the leads app, then redeploy:

**Required new vars:**
- `GOOGLE_SERVICE_ACCOUNT_EMAIL` — the service account email from GCP (ends in @...iam.gserviceaccount.com)
- `GOOGLE_PRIVATE_KEY` — the private_key value from the service account JSON. Keep the `\n` as literal backslash-n (do NOT paste real newlines). Example format: `-----BEGIN RSA PRIVATE KEY-----\nMIIEo...\n-----END RSA PRIVATE KEY-----\n`
- `CRON_SECRET` — generate with `openssl rand -hex 32`, paste the output

**Steps:**
1. SSH to VPS or open Easypanel at http://178.104.14.40:3000
2. Navigate to the leads app service → Environment
3. Add the three vars above
4. Click Save / Redeploy
5. Wait for the build to complete (watch logs for "Ready on http://0.0.0.0:80")
6. Also: open the target Google Spreadsheet and share it with GOOGLE_SERVICE_ACCOUNT_EMAIL as Editor

**Separately: set up cron at cron-job.org (free, no account needed — just email):**
1. Go to https://cron-job.org → Create cronjob
2. URL: `https://leads.esqtools.com/api/cron/retry`
3. Method: GET
4. Add custom header: `X-Cron-Secret: {your CRON_SECRET value}`
5. Schedule: every 5 minutes
6. Save
  </what-to-do>
  <resume-signal>Type "deployed" when the service is back up and cron is configured.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Update test source with sheetsId + sheetTab, then E2E curl verify</name>
  <read_first>
    - .planning/STATE.md — test source slug="test", token="test-webhook-token-change-me"
    - .planning/phases/02-webhook-ingestion/02-03-SUMMARY.md — existing curl patterns
  </read_first>
  <files>(no source files — runs SQL and curl only)</files>
  <action>
**Step 1 — Update test source in Supabase SQL Editor with real Sheets config:**
```sql
UPDATE "Source"
SET
  "sheetsId"  = 'YOUR_SPREADSHEET_ID',
  "sheetTab"  = 'Leads',
  "dataCrazyUrl" = NULL
WHERE slug = 'test';
```
Replace `YOUR_SPREADSHEET_ID` with the actual Google Sheets spreadsheet ID (the long string in the sheet URL between `/d/` and `/edit`).

Note: dataCrazyUrl is set to NULL for the test source to avoid failures on an unconfigured endpoint.

**Step 2 — POST a test lead via curl:**
```bash
curl -s -X POST https://leads.esqtools.com/api/webhook/test \
  -H "Content-Type: application/json" \
  -H "X-Webhook-Token: test-webhook-token-change-me" \
  -d '{"name":"Teste INT-01","email":"teste@esqtools.com","phone":"11999990000","utm_source":"curl-test"}' | jq .
```
Expected: `{"id":"<cuid>"}` with HTTP 200.

**Step 3 — Check Google Sheets:** Open the spreadsheet → Leads tab → confirm a new row appeared with name "Teste INT-01".

**Step 4 — Check SyncLog in Supabase:**
```sql
SELECT id, destination, status, error, "attemptCount"
FROM "SyncLog"
ORDER BY "attemptedAt" DESC
LIMIT 5;
```
Expected: sheets row with status='done'; datacrazy row with status='done' (skipped since dataCrazyUrl is NULL — or status='done' if trigger treats null config as immediate success).

**Step 5 — Verify cron route is protected:**
```bash
curl -s -o /dev/null -w "%{http_code}" https://leads.esqtools.com/api/cron/retry
```
Expected: 401

**Step 6 — Verify cron route is accessible with secret:**
```bash
curl -s https://leads.esqtools.com/api/cron/retry \
  -H "X-Cron-Secret: YOUR_CRON_SECRET" | jq .
```
Expected: `{"processed":0,"total":0}` (no failed rows to retry)

**Step 7 — Verify /api/leads returns sync status:**
```bash
# First obtain a session cookie (use browser or curl login)
# Or use the leads page in the app to confirm data appears
curl -s https://leads.esqtools.com/api/leads \
  -H "Cookie: YOUR_SESSION_COOKIE" | jq '.leads[0] | {id, status, syncLogs}'
```
Expected: lead object with syncLogs array containing sheets and datacrazy entries.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" https://leads.esqtools.com/api/cron/retry</automated>
  </verify>
  <done>
- HTTP 200 returned on webhook POST with valid lead
- New row visible in Google Sheets
- SyncLog shows sheets=done for the new lead
- /api/cron/retry returns 401 without secret, 200 with secret
- /api/leads returns lead list with syncLogs array
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: INT-04 — confirm sync status visible in lead list</name>
  <what-built>
    GET /api/leads endpoint returning syncLogs per lead (implemented in Plan 03). This checkpoint verifies the data is correct before Phase 3 is closed.
  </what-built>
  <how-to-verify>
    1. Open https://leads.esqtools.com in your browser and log in
    2. If the dashboard has a leads section (may be minimal in Phase 3 — the API is the deliverable): confirm the page loads without errors
    3. Run curl from Task 2 Step 7 and inspect the response — each lead should have syncLogs with destination and status fields
    4. For the test lead posted in Task 2: confirm sheets status = "done"
  </how-to-verify>
  <resume-signal>Type "verified" if INT-04 data is correct, or describe what's missing.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron-job.org → /api/cron/retry | External HTTP caller; authenticated via X-Cron-Secret |
| Developer → Easypanel env vars | Credentials injected at deploy time; not in git |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Information Disclosure | GOOGLE_PRIVATE_KEY in Easypanel UI | accept | Easypanel restricts env var access to admin; key is never committed to git |
| T-03-04-02 | Elevation of Privilege | cron-job.org as unverified caller | accept | X-Cron-Secret provides sufficient access control for a low-sensitivity retry operation |
</threat_model>

<verification>
All four INT requirements verified:
- INT-01: New row in Google Sheets after curl POST
- INT-02: SyncLog for datacrazy destination exists (done or failed recorded)
- INT-03: /api/cron/retry responds 200 with secret; cron-job.org configured to call every 5 min
- INT-04: /api/leads returns syncLogs per lead with destination + status fields
</verification>

<success_criteria>
- GOOGLE_SERVICE_ACCOUNT_EMAIL, GOOGLE_PRIVATE_KEY, CRON_SECRET injected in Easypanel and deployed
- Test lead POSTed via curl → new row appears in Google Sheets
- /api/cron/retry returns 401 without X-Cron-Secret, 200 with correct secret
- /api/leads returns lead list with syncLogs array (destination, status fields present)
- cron-job.org configured to call /api/cron/retry every 5 minutes
</success_criteria>

<output>
After completion, create `.planning/phases/03-integrations/03-04-SUMMARY.md`
</output>
